{{#if stat}}
  ### Bundle Impact Report 📈

  {{#if (eq impact 0)}}🟢 No Impact{{/if}}{{#if (eq impact 1)}}🟡 Low Impact{{/if}}{{#if (eq impact 2)}}🟠 Medium Impact{{/if}}{{#if (eq impact 3)}}🔴 High Impact{{/if}}

  {{#each stat}}
- {{ @key }} ({{ this.filename }}) **{{ this.delta.gzip }}** gzipped
  - **[{{or this.tag this.branch}}]** - **{{ filesize gzip }}** ({{ filesize size }}, {{percent compressed}})
    {{#each history}}
  - [{{ or this.tag this.branch }}] - {{ filesize gzip }} ({{ filesize size }}, {{percent compressed}})
    {{/each}}
  {{/each}}
{{/if}}

{{#if warns}}
#### Warnings
  {{#each warns}}
    {{#if sizeReport}}
- ⚠️ File `{{filename}}` has increased in size by `{{filesize diff}}` (`+{{percent percent}}`)
    {{/if}}
  {{/each}}
{{/if}}
